let number;

// 예측 가능하고 디버깅하기 쉬운!!
// 연산자 우선 순위 (((x+y) * z))

function decrement(number) {
  number--;

  number = number - 1;
}

function increment(number) {
  number++;

  number = number + 1;
}

setTimeout(() => {}, timeout);

while (condition) {
  number++;
}
